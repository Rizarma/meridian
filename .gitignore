# Dependencies
node_modules/
dist/
*.tsbuildinfo
package-lock.json
yarn.lock
bun.lock

# Environment & secrets
.env
.env.*
!.env.example

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
signal-weights.json
.repl_history

# SQLite database (migrated from JSON files)
meridian.db
meridian.db-shm
meridian.db-wal
*.db-journal

# Auto-generated backups
backups/

# Generated reports
report/


# Logs
logs/

# OS
.DS_Store

# Dev context
context.md

# Plan folder — local only, never push to GitHub
plan/

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code — only track shared settings, not local overrides
.claude/scheduled_tasks.lock
.claude/settings.local.json

# CI-generated files
audit-results.json
violations.json
violations.txt

# Coverage reports
coverage/

# Husky internal files
.husky/_
*.json.backup
local-backup.json
